# Routing & Navigation

## Configuration
- Define all routes in `src/router/routes.ts`.
- Routing Mode: Hash-based routing (configured in `quasar.config.js`).
- Base URL: `/gkapologista-portfolio/`.

## Orchestration
- Use `vue-router` for all navigation.
- Programmatic navigation should use `useRouter`.
